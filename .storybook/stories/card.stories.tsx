import { DefaultCard } from "@/examples/components/card/default-card";

const CardMeta = {
  title: "Components/Card",
};

export default CardMeta;

export const WithImage = {
  render: () => {
    return <DefaultCard />;
  },
};
