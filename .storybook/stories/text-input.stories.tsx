import { DefaultTextInputExample } from "@/examples/components/text-input/default";

const TextInputMeta = {
  title: "Components/TextInput",
};

export default TextInputMeta;

export const Default = {
  render: (args) => {
    return <DefaultTextInputExample />;
  },
};
