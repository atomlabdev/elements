import { ProgressBarExample } from "@/examples/components/progress-bar/progress-bar";

const ProgressBarMeta = {
  title: "Components/Progress Bar",
};

export default ProgressBarMeta;

export const Default = {
  render: () => {
    return <ProgressBarExample />;
  },
};
