import { Box } from "@/components/box";
import { Text } from "react-native";

export const DestructiveBox = () => {
  return (
    <Box variant="destructive">
      <Text>This is a destructive box</Text>
    </Box>
  );
};
