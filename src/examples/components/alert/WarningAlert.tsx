import { Alert } from "@/components/Alert";

export const WarningAlert = () => {
  return (
    <Alert
      variant="warning"
      title="Warning Alert"
      description="This is an alert"
    />
  );
};
